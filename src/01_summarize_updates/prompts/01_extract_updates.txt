In the attached powerpoint presentation there are proposed updates to the GSBPM phases. I'll include these phases in a structured JSON format that includes descriptions. From the powerpoint document extract the anticipated updates to the GSBPM phases and sub-phases. The slides following a header slide that include the words "GSBPM Roadmap [...]" contain the bulk of the relevant information. Extract the updates and report them sequentially in a JSON format that first references where and/or how the update was identified, includes the extracted update text (quote from the presentation), specifies which strategic priorities are targeted (if any), and identifies which phase(s) or sub-phase(s) are implicated. The structure of phases is a list of phase/sub-phase justification objects where first there is a concise justification for why the phase is implicated, then the phase id and name. The structure looks like this:

```{JSON schema for extracted updates}
[
  {
    "update": {
        "reference": <SLIDE NUMBER> - <SLIDE TITLE>,
        "description": <EXTRACTED TEXT REGARDING UPDATE>,
	"strategic priorities" : [<ANY CORRESPONDING STRATEGIC PRIORITIES>],
        "implicated phases and subphases": [
            {
                "justification": <FIRST EVIDENCE THEN POSSIBLY REASONING FOR IMPLICATION>,
                "text": <PHASE OR SUB-PHASE TEXT>,
                "id" <PHASE OR SUB-PHASE ID>
            },
	    ...
        ]
    },
...
}
```
Some updates specify which phases or sub-phases are implicated. However others don't, therefore use your reasoning to identify any or any additional affected phases or subphases. If no phases or sub-phases can be justified as implicated for a given update, then provide a brief justification. Lastly, Don't consolidate distinct update points into a single update. Report each seperately.


Here's the JSON that details the GSBPM phases
```{JSON}
{{gsbpm_structured.json}}
```
Before you attempt the task, is this clear? ask any clarifying questions you have.
