Extract data from this markdown text into JSON. The JSON structure can be determined from an HTML table embbedded in the document. This HTML table follows text (possibly a header): "Levels 1 and 2 of the GSBPM". The structure should be one overarching list containing an object for each column. Each column-object should contain an "phase" attribute that contains its id, its title, a "description" attribute containing text describing the phase, and an attribute called "sub-processes" that refers to a list containing objects that refer to the items in that column. Each "sub-process" object should have the following attributes: "id" for the numerical id, "text" for the title of the sub-process, and "description" for the description of the sub-process.

The text for the description of the phases and sub-process needs to be extracted from some the "Descriptions of Phases and Sub-processes" section. Do not summarize or truncate the description, I want the exact text from the markdown document. Please do remove newline characters. I want the text, not the formatting.
